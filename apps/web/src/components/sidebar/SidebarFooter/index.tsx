import { type ReactElement } from 'react'

// MOVA: sidebar footer hidden (Use prod CGW, indexing status, What's new, Help)
const SidebarFooter = (): ReactElement | null => null

export default SidebarFooter
